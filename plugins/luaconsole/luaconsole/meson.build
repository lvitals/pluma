lua_files = [
  'init.lua',
  'console.lua',
  'config.lua',
  'compat.lua',
]

install_data(lua_files,
  install_dir: join_paths(pkglibdir, 'plugins/luaconsole')
)

install_data('config.ui',
  install_dir: join_paths(pkgdatadir, 'plugins/luaconsole/ui')
)
